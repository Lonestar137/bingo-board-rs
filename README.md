# Bingo Board

```bash
cargo add yew --features csr
cargo add wasm-bindgen

# install trunk
cargo install trunk
  
```

Building and serving the app
```bash
# Manual build
trunk build

# Hot reloads
trunk serve
```
